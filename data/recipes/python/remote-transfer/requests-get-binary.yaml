title: Downloading a binary file via Requests and GET
description: Use the requests.Response `content` attribute
lede:
  kind: snippet
  lang: python
  code: |
    import requests
    resp = requests.get("http://www.example.com/image.jpg")
    stuff = resp.content
