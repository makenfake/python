title: Downloading a text file via Requests and GET
description: Use the requests.Response `text` attribute
lede:
  kind: snippet
  lang: python
  code: |
    import requests
    resp = requests.get("http://www.example.com")
    stuff = resp.text

references:
  - url: http://docs.python-requests.org/en/master/user/quickstart/#response-content
    title: Requests Quickstart - Response Content
