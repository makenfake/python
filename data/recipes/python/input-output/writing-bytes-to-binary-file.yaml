title: Writing bytes to a binary file
description: ''
lede:
  kind: snippet
  description: |
    Provide `wb` as the second argument to the `open()` call
  lang: python
  code: |
    my_bytes = b'10101'
    with open("myfilename.data", "wb") as outfile:
        outfile.write(my_bytes)

