title: Writing bytes to a binary file
description: 'Provide wb as the argument'
lede:
  kind: snippet
  lang: python
  code: |
    my_bytes = b'12345'
    with open("myfilename.data", "wb") as outfile:
        outfile.write(my_bytes)

summary: |
  - "The filename and its extension are irrelevant"
